# DESIGN Evolution

本文件保存 `DESIGN.md` 主线之外仍有长期价值的设计理由、边界来源和候选方向。`DESIGN.md` 只描述当前运行时契约。

## 稳定设计护栏

- 2026-09-04: 初始设计以“随机 heartbeat + 确定性 final-response review”验证多个持久职责 Shadow 是否能够稳定补充 Main。复杂智能调度会改变实验变量，因此留待基础机制获得数据后再评估。
- 2026-09-04: Shadow 定义采用全局 registry，运行实例保持一次性。全局实体先验证跨项目复用价值；临时 AgentSession 则保持实例隔离，避免隐式继承旧运行历史。
- 2026-09-04: 工具授权以用户显式白名单为边界。Pi 的通用 `ToolDefinition` 无法声明只读性，插件也无法完整预知第三方工具副作用，因此运行时不制造“文件写入已被安全互斥”的假保证。
- 2026-09-04: Shadow 上下文采用 Main 可见上下文的净化子集。它保留用户约束、Main 行为和工具调用概述，同时排除 Main thinking、完整工具结果与未上报的 Shadow 推理，使 Shadow 能独立核查而不是复制 Main 的证据链。
- 2026-09-04: 完成后检查由独立 generation 聚合整批结果。新输入或修订会使旧 generation 失效，防止较慢的检查在 Main 已经进入下一条任务线后继续介入。
- 2026-09-04: `report_to_main` 保持单字段、单次上报协议。严重度、类别和表达结构属于各 Shadow 的职责定义，公共运行时只负责终止当前 loop 与可靠投递。

## 候选方向

- 基于实际收益、误报、成本和延迟数据，评估语义 Gate、Expected Value of Thinking 或学习型调度器。
- 当项目级职责出现稳定需求时，设计项目 registry 及全局/项目覆盖规则。
- 为能够声明目标文件的工具设计自愿接入的协作式锁；未知工具继续明确处于协议之外。
- 在不削弱独立核查的前提下，评估跨次 Shadow 记忆或显式 Shadow 间通信。
- 当不同执行模型的上下文窗口形成真实限制时，评估独立摘要或截断策略。
- 根据跨供应商运行的实际隐私需求，评估工具参数中的凭据识别与脱敏。
- 当观察需求成熟后，评估内置 Markdown 编辑体验以及调试日志轮转策略。

## 延后路径

- 依据错误、风险、计划变化等语义事件直接进行规则调度。
- 自动训练或优化 Shadow 及其调度策略。
- 把 Main thinking 或完整工具结果直接传递给 Shadow。
- 由运行时强制所有 Main、Shadow 与第三方工具参加文件级写入锁。
- 用模型对同期 Shadow 报告进行二次总结或去重。
- 在公共报告协议中加入固定严重度、类别或报告模板。

## 退役表达

- 将“多核”或“章鱼”隐喻作为运行时技术定义。它们可以服务产品表达，但不拥有架构语义。
